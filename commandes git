git pull // récupérer les données que les autres ont fait


// mettre à jour ce que tu as fait sur le projet être dans le dossier mobile-project
git add .
git commit -m "J'ai ajouté le petit bouton par ex" 
git push

// toujours rebuild l’app iOS dans le fichier Hazel-studio
Cordova build iOS
